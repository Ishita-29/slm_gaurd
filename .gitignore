# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/

# Environment
.env

# Logs (may contain HF xet-read-tokens from HTTP request logging)
logs/
*.log
slmguard/logs/

# Backup files
*.backup

# IDE
.vscode/


# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Model weight files — too large for git, reproducible via training scripts.
# Code-only repo: dataset and checkpoints are published/distributed separately.
*.bin
slmguard/checkpoints/

# Keep small experiment result JSONs even though checkpoints/ is excluded above
!slmguard/checkpoints/slmguard-v1/baseline_results.json
!slmguard/checkpoints/slmguard-v1/guardrail_comparison.json
!slmguard/checkpoints/slmguard-v1/eval_results.json

# Datasets — code-only repo, data published separately (e.g. HuggingFace Hub)
data/
slmguard/data/

# HuggingFace datasets internal cache files (in case any data/ exception is ever added)
**/cache-*.arrow


# Installers
Miniconda*.sh
